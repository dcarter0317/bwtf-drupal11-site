name: Deploy to NameHero Staging

on:
  push:
    branches:
      - main  # Change to your default branch if needed

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      ACTIONS_STEP_DEBUG: true
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.IDEAPAD_ACTIONS_FOR_DEPLOYMENT }}

      - name: Debug Git Configuration
        run: |
          ssh -o StrictHostKeyChecking=no -p 37980 ${{ secrets.NAMEHERO_USER }}@${{ secrets.NAMEHERO_HOST }} 'cd /home/bwtfcom/stage.bwtf.com && git remote -v'

      - name: Pull latest code on server
        run: |
          ssh -o StrictHostKeyChecking=no -p 37980 ${{ secrets.NAMEHERO_USER }}@${{ secrets.NAMEHERO_HOST }} 'cd /home/bwtfcom/stage.bwtf.com && git pull'

      - name: Ensure destination directory exists
        run: |
          ssh -o StrictHostKeyChecking=no -p 37980 ${{ secrets.NAMEHERO_USER }}@${{ secrets.NAMEHERO_HOST }} 'mkdir -p /home/bwtfcom/stage.bwtf.com/web/sites/default/files'

      - name: SCP files directory to server
        run: |
          scp -o StrictHostKeyChecking=no -r web/sites/default/files ${{ secrets.NAMEHERO_USER }}@${{ secrets.NAMEHERO_HOST }}:/home/bwtfcom/stage.bwtf.com/web/sites/default/

      - name: Clear Drupal cache (optional)
        run: |
          ssh -o StrictHostKeyChecking=no -p 37980 ${{ secrets.NAMEHERO_USER }}@${{ secrets.NAMEHERO_HOST }} 'cd /home/bwtfcom/stage.bwtf.com/web && php core/scripts/drupal cache:rebuild || true'